import { SmokerDashboard } from './components/SmokerDashboard';
import './App.css'; // Keep this if Vite created it, or remove if empty/unnecessary

function App() {
  return (
    <div className="dark">
      <SmokerDashboard />
    </div>
  );
}

export default App;
